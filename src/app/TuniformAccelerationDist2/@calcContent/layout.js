export const metadata = {
  title: "Uniform acceleration distance",
  description: "Uniform acceleration distance",
};

const UniformAccelerationDistLayout2 = (props) => {
  return (
    <div>
      <section>{props.children}</section>
      {/* @textContent */}
      {/* <section>{props.textContent}</section> */}
    </div>
  );
};

export default UniformAccelerationDistLayout2;
