export const metadata = {
  title: "Uniform ance",
  description: "Unifo",
};

const  ShareLayout = ({ children }) => {

  // const f = NEXT_PUBLIC_URL;
  return <>{children}</>;
};

export default  ShareLayout;