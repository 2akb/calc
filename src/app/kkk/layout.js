// "use state";

export const metadata = {
  title: "Uniform ance",
  description: "Unifo",
};

const CwiczebnyLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default CwiczebnyLayout;
